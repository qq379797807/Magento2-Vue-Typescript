import { ThemeConfig } from '../packages'

const themeConfig: ThemeConfig = {
    default: {
        name: 'vue',
        area: 'frontend',
        src: 'Restful/vue',
        locale: 'en_US',
        parent: '',
        mode: 'development',
        styles: 'scss',
        scripts: 'tsx',
        files: [
            'css/app.css'
        ]
    }
}

export default themeConfig